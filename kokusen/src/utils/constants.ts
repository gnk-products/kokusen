// default match map count for new match seeds
export const DEFAULT_MATCH_MAP_COUNT = 5;

// min match map count
export const MIN_MATCH_MAP_COUNT = 5;

// max match map count
export const MAX_MATCH_MAP_COUNT = 30;

// match map count step
export const MATCH_MAP_COUNT_STEP = 5;

// api timeout
export const API_TIMEOUT_SECONDS = 20;

// z-index list
export const Z_INDICES = {
  PAGE_HEADER: 1000,
  MATCH_CARD_ELEMENT: 1,
  MATCH_ANSWER_ACTION: 100,
  OVERLAY: 10000,
};
